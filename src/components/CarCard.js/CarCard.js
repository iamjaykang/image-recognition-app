import React from "react";
import "./CarCard.css";
import CarContent from "../CarContent/CarContent";

const CarCard = ({ carItem }) => {
  return (
    <div>
      {/* <div className="description">{carItem && carItem.name}</div> */}
    </div>
  );
};

export default CarCard;
