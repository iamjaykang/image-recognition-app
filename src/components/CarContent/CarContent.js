import React from "react";
import CarCard from "../CarCard.js/CarCard";

const CarContent = ({ carArray }) => {
  return (
    <div>
      {/* {carArray && carArray.map((carItem, index) => (
        <CarCard userItem={carItem} key={index} index={index} />
      ))} */}
    </div>
  );
};

export default CarContent;
